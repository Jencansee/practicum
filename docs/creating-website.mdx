---
title: 'Разработка сайта с HTML, CSS'
section: Создание и преобразование информационных объектов
---

# HTML, CSS - каскадные таблицы стилей. Внедрение мультимедиа на веб страницу

Давайте создадим HTML файл где будем верстать нашу страницу.

Создайте файл с названием `index.html` и добавьте в него следующий контент:

```
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Название страницы</title>
</head>
<body>

</body>
</html>
```

Существуют разные версии HTML. Параметр doctype\* - \*`<!DOCTYPE html>` указывает, что этот HTML-документ содержит код HTML5.

Мы не собираемся углубляться в значения всех элементов HTML, однако укажем на несколько важных моментов. Тег `meta` указывает на *метаданные*, которые обычно видны пользователю, только если он просматривает исходный код в браузере. Метаэлементы или теги содержат описательную информацию о веб-странице. Метаданные помогают поисковым системам определять, какие сведения на веб-страницах должны возвращаться в результатах поиска.

*Кодировка* (`charset`) для UTF-8 может показаться незначительным параметром, но без него компьютеры не смогут правильно обрабатывать символы. Если метаданные для кодировки (charset) отсутствуют, это может привести к нарушению безопасности

## Изменение заголовка

Элемент `<head>` в коде HTML содержит сведения о веб-сайте, которые не отображаются на вкладке браузера.

Атрибут *metadata* определяет данные о HTML-документе, такие как набор символов, скрипты и браузер, в котором открывается веб-страница.

Атрибут *title* содержит название веб-страницы, которое отображается в верхней части окна браузера. Оно важно по множеству причин. Например, название используется поисковыми системами и отображается в них. Давайте добавим название.

В редакторе измените элемент `<title>` так, чтобы он соответствовал следующему примеру.

```html
....
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Впишите сюда свое название</title>
....
```

Чтобы применить стили элементов HTML к веб-странице, код CSS можно включить непосредственно в заголовок этой веб-страницы. Написание CSS в коде страницы HTML называется *внутренним CSS*. Однако рекомендуется разделять структуру HTML и стили CSS. Если код CSS вынесен на отдельную страницу, он называется *внешним CSS*. Лаконичный и систематизированный код проще читать. Одну внешнюю таблицу стилей можно использовать для нескольких веб-страниц. Чтобы не использовать повторяющийся код CSS на каждой странице HTML, вы можете внести изменения в файл CSS, и они будут применены ко всем зависимым веб-страницам. Давайте свяжем страницу с внешней таблицей стилей

```html
<link rel="stylesheet" href="">
```

1.  Создайте файл `main.css` в том же репозитории как и `index.html`
2.  Измените href= на `href="main.css"` и сохраните файл

```
  .....
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Впишите сюда свое название</title>
  <link rel="stylesheet" href="main.css">
  ....
```

## Изменение тела

Теперь начнем заполнять элемент `<body>`.

Элемент `<body>` включает содержимое веб-сайта, отображаемое в браузерах клиентов.

1.  Добавьте элемент `<h1>`*заголовка*, за которым следует элемент `<p>`*абзаца*, а затем создайте *неупорядоченный список*`<ul>`, содержащий несколько *элементов списка*`<li>`.
2.  Измените код или скопируйте его и вставьте, чтобы он выглядел следующим образом:

```
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Название сайта</title>
      <link rel="stylesheet" href="main.css">
  </head>
  <body>
    <h1>Task List</h1>
    <p id="msg">Текущие задачи</p>
    <ul>
      <li class="list">Добавить стили на сайт</li>
      <li class="list">Добавить изображение</li>
    </ul>
  </body>
</html>
```

Атрибут ID (используемый в элементе `<p>`) можно использовать для определения стиля одного элемента, а атрибут class (используемый в элементе `<li>`) предназначен для стилизации всех элементов одного и того же класса.

Перед следующим шагом убедитесь, что файл сохранен.

# Настройка стилей HTML с использованием CSS

Каскадные таблицы стилей (CSS) позволяют настраивать внешний вид страницы. Основная идея заключается в том, чтобы определить стиль для элементов, используемых на страницах HTML. Если элементы HTML определяют само содержимое, то стили CSS определяют внешний вид содержимого.

Например, с помощью стилей CSS можно применить к определенному элементу скругленные углы или градиентный фон. Или можно указать, как должны выглядеть гиперссылки и как они должны изменяться при взаимодействии с ними. Стили CSS также позволяют создавать сложные макеты и реализовывать сложные эффекты анимации.

Вы можете применять стили к конкретным элементам, ко всем элементам заданного типа или использовать классы для применения стилей к нескольким различным элементам.

## Правила CSS

Правила CSS определяют применение стилей к элементам HTML. Правила CSS имеют **селектор**, который указывает элемент или элементы, к которым должны применяться стили.

Откройте файл `main.css` и введите приведенный ниже код.

```
body {
    font-family: monospace;
}

ul {
    font-family: helvetica;
}
```

Приведенный выше фрагмент кода содержит два правила. Каждое правило содержит следующие элементы:

*   *Селектор*. `body` и `ul` представляют собой селекторы двух правил и используются для выбора элементов, к которым применяются стили.
*   Открывающая фигурная скобка (`{`).
*   Список *объявлений стилей*, определяющих, как должны выглядеть выбранные элементы.
*   Закрывающая фигурная скобка (`}`).

Например, селектор `ul` выбирает элемент HTML `<ul>` на странице, чтобы применить к нему стили. `font-family: helvetica` — это объявление, которое определяет стиль. `font-family` — это *имя свойства*, а `helvetica` — *значение*.

Как вы увидите далее, элементам можно присваивать пользовательские имена.\\

## Селекторы

Селекторы *идентификаторов* и *классов* позволяют применять стили к настраиваемым атрибутам в коде HTML. Идентификатор позволяет настроить стиль для одного элемента, а класс — сразу для нескольких элементов.

1.  Скопируйте следующий код в файл CSS после закрывающей фигурной скобки для селектора `ul`, добавленного ранее.

```
li {
  list-style: circle;
}

.list {
  list-style: square;
}

#msg {
  font-family: monospace;
}

img {
  width: 250px;
  height: 250px;
}
```

Приведенный выше код содержит три правила CSS. В двух последних правилах используются настраиваемые атрибуты для выбора элементов: `.list` и `#msg`. Также сделаем заготовку на наше изображение `img` мы ему задали размер 250x250 пикселей.

*   `.list` представляет собой *селектор класса*. Каждый элемент HTML, содержащий атрибут `class` со значением `list`, будет наследовать стили, определенные в этом селекторе.
*   `#msg` представляет собой *селектор идентификатора*. Каждый элемент HTML, содержащий атрибут `id` со значением `msg`, будет наследовать стили, определенные в этом селекторе.

Используемые для селекторов имена могут быть произвольными, но должны соответствовать именам, определенным в HTML.

![](https://res.cloudinary.com/dzfsp6wbp/image/upload/v1654026349/applied-font-styles\_fev2pm.png "")

### Добавляем изображение

В репозиторий с `index.html` перенесите любое `jpg` изображение и переименуйте его в `image`. И переносимся во внутрь `index.htmд`

Добавьте следующий код:&#x20;

```
  <body>
    <h1>Task List</h1>
    <img src="image.jpg" />
    <p id="msg">Текущие задачи</p>
    <ul>
      <li class="list">Добавить стили на сайт</li>
      <li class="list">Добавить изображение</li>
    </ul>
  </body>
```

И теперь на вашей странице появилось изображение!
